

// window.addEventListener('resize', resizeTuiGrid);
// function resizeTuiGrid(){
//     if(!(instance == undefined || instance == null || instance ==''))
//         instance.refreshLayout();
//     if(!(state.gridInstances == [] || state.gridInstances == undefined || state.gridInstances == null))
//         state.gridInstances.forEach((instanceEl)=>{ instanceEl.refreshLayout(); });
// }